@{
    if (!ViewContext.RouteData.Values["area"]?.ToString()?.Equals("Identity", StringComparison.OrdinalIgnoreCase) ?? true)
    {
        Layout = "/Views/Shared/_Layout.cshtml";
    }
    else
    {
        Layout = null;
    }
}